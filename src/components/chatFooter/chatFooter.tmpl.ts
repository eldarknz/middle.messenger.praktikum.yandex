// language=hbs

export default `
    <div class="chat-footer__container">
        <div class="chat-footer__container__content">
            {{{ dropdown }}}
            {{{ inputMessage }}}
            {{{ buttonSend }}}
        </div>
    </div>
`

// language=hbs

export default `
    <div class="wrapper">
        {{{ sidebar }}}
        <div class="chat">
            {{{ footer }}}
            {{{ messageArea}}}
            {{{ header }}}
        </div>
    </div>
`
